<Window x:Class="NetStrata.Tray.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetStrata 设置"
        Height="480" Width="520"
        Background="#0F1115"
        Foreground="#E8EAED"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="140"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="config.json" FontWeight="SemiBold" Margin="0,0,0,12"/>

    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="探测间隔 (ms)"/>
    <TextBox x:Name="IntervalBox" Grid.Row="1" Grid.Column="1" Margin="0,4" Padding="6"/>

    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Web 端口"/>
    <TextBox x:Name="PortBox" Grid.Row="2" Grid.Column="1" Margin="0,4" Padding="6"/>

    <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="0,8,0,0" Text="额外 Ping"/>
    <TextBox x:Name="PingExtraBox" Grid.Row="3" Grid.Column="1" Margin="0,4" Padding="6"
             ToolTip="逗号分隔，最多 10 个"/>

    <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" Margin="0,8,0,0" Text="Ping 标签"/>
    <TextBox x:Name="PingLabelsBox" Grid.Row="4" Grid.Column="1" Margin="0,4" Padding="6" Height="72"
             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
             ToolTip="每行 ip=label"/>

    <TextBlock Grid.Row="5" Grid.Column="0" VerticalAlignment="Top" Margin="0,8,0,0" Text="TLS 栈目标"/>
    <TextBox x:Name="TlsTargetsBox" Grid.Row="5" Grid.Column="1" Margin="0,4" Padding="6"
             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

    <TextBlock x:Name="StatusText" Grid.Row="6" Grid.ColumnSpan="2" Margin="0,8,0,0" Foreground="#9AA0A6"/>

    <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="取消" Margin="0,0,8,0" Padding="14,6" Click="Cancel_Click"/>
      <Button Content="保存" Padding="14,6" Click="Save_Click"/>
    </StackPanel>
  </Grid>
</Window>
